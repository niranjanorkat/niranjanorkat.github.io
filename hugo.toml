baseURL = 'https://niranjanorkat.github.io/'
languageCode = 'en-us'
title = 'Niranjan Orkat'
theme = 'gokarna'

[menu]
  [[menu.main]]
    # Display name
    name = "Posts"

    # Relative URL slug (appended to baseURL)
    url = "/posts/"

    # Lower weights are listed first in the menu (leftmost); higher weights are
    # listed last in the menu (rightmost)
    weight = 1


  [[menu.main]]
    name = "Projects"
    url = "/projects/"
    weight = 2

  [[menu.main]]
    name = "About"
    url = "/about/"
    weight = 3
[params]
showpostsonhomepage = "recent"
NumberPostsOnHomePage = 4 